#include <stdio.h>
//a pointer is a variable whose value is address of another variable
int main(){
	int p1;
	int p2[10];
	printf("address of p1 is %x\n",&p1);
	printf("address of p2 is %x\n",&p2);
	
	return 0;
	}
